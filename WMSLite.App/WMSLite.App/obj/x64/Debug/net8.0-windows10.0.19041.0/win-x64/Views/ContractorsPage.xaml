<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="WMSLite.App.Views.ContractorsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WMSLite.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton x:ConnectionId='3' x:Name="AddButton" Icon="Add" Label="Dodaj"                        />
            <AppBarButton x:ConnectionId='4' x:Name="EditButton" Icon="Edit" Label="Edytuj"                         />
        </CommandBar>

        <controls:DataGrid x:ConnectionId='2' x:Name="ContractorsGrid"
                           Grid.Row="1"
                                                                                    
                           AutoGenerateColumns="False"
                           CanUserSortColumns="True"
                           IsReadOnly="True"
                           SelectionMode="Single"
                           HeadersVisibility="Column"
                           GridLinesVisibility="Horizontal">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="SizeToCells"/>
                <controls:DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

    </Grid>
</Page>

